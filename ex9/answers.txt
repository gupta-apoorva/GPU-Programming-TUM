when tau is large :: The picture becomes very grainy (iterations number was 10)

when N is very large :: Could not see anything in the image...very very blurry

Compare results with gaussian convolution :: Gaussian was smooth this was a bit grainy.

For running the code just go into the build_cmake folder and run the script.sh file by changing the values into it.
